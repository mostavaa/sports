@model PresentationLayer.Models.dbModels.Attachment

@{
    ViewBag.Title = "title";
}

<div id="app" class="container">




    @using (Html.BeginForm("Create", "Attachment", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        @Html.AntiForgeryToken()

        <div class="form-horizontal">
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            <table class="table">
                <thead>
                <tr>
                    <th>
                        @Html.LabelFor(model => model.AlbumId, htmlAttributes: new { @class = "control-label col-md-2" })
                    </th>
                    <th>
                        <label class="form-label">Image</label>

                    </th>

                      


                </tr>
                </thead>
                <tbody>
                <tr>
                    <td>
                        @Html.DropDownListFor(model => model.AlbumId, (IEnumerable<SelectListItem>)ViewBag.Albums, new { @class = "form-control" , required="required"})
                        @Html.ValidationMessageFor(model => model.AlbumId, "", new { @class = "text-danger" })
                    </td>
                    <td>
                        <div class="col-md-6">
                            <div class="form-group">
                                <input required type='file' id="file" name="file" class="form-control" accept="image/*"  />
                            </div>
                        </div>

                    </td>
                </tr>
                </tbody>
            </table>
            <div class="">
                <div class="col-md-offset-2 col-md-2">
                    <input type="submit" value="Create" class="btn btn-default" />
                </div>
            </div>
        </div>
    }
    <hr />
    <div>
        @Html.ActionLink("Back to List", "Index")
    </div>
</div>
@section scripts
{
    <script>
        var app = new Vue({
            el: '#app',
            data: {
            },
            created: function () {
         
            },
            methods:
            {

            }
        });
    </script>
}
